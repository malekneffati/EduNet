name: edunet
description: "EduNet Flutter Application"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.2
  firebase_storage: ^12.3.7
  google_sign_in: ^6.3.0
  provider: ^6.1.1
  flutter_riverpod: ^2.4.0
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.5.0
  go_router: ^13.0.0
  file_picker: ^8.1.6
  intl: ^0.19.0
  fl_chart: ^0.68.0
  video_player: ^2.8.1
  url_launcher: ^6.2.1
  http: ^1.2.0
  chewie: ^1.13.0
  app_links: ^6.3.4
  webview_flutter: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true